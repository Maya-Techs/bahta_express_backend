name: 🚀 Deploy Bahta Express Backend

on:
  push:
    branches:
      - master

jobs:
  web-deploy:
    name: 🎉🎉🎉🎉🎉🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂📂📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.GIT_CPANEL_BACKEND_SERVER }}
          username: ${{ secrets.GIT_CPANEL_BACKEND_USER }}
          password: ${{ secrets.GIT_CPANEL_BACKEND_PWD }}
          protocol: ${{ secrets.GIT_CPANEL_BACKEND_PROTOCOL }}
          local-dir: ./
